-- A script that deletes the database 'hbtn_0c_0' in MySQL server
-- If the database doesn’t exist, script should not fail
-- Not allowed to use the SELECT or SHOW statements
DROP DATABASE IF EXISTS hbtc_0c_0;
